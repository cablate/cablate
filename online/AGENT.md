
[IMPORTANT]
1. 搜尋前應先彙整所有關鍵字，一次完成搜尋，不得逐次呼叫工具。
2. 構築指令之前要注意操作是否有足夠的信心完成需求，不要瘋狂嘗試耗費我的調用次數。
3. 總是使用繁體中文回覆，而不是英文。

正確：
FileSearch -Path file.html -Pattern "XSS|CSRF|Token|DevTool"

錯誤：
FileSearch -Path file.html -Pattern "XSS"
FileSearch -Path file.html -Pattern "CSRF"
...
